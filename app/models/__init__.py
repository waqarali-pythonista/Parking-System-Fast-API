# app/models/__init__.py

# This file can be left empty or used to import models to make them accessible at the package level.
# By importing models here, we can use `from app.models import User, ParkingArea` elsewhere in the application.

from .user import User
from .parking_area import ParkingArea
from .booking import Booking
